/* 
Write a loop that goes from 1 to 100, but stops (using break) when it encounters the first square number (like 4, 9, 16, etc.)
*/

//Break Method
for (let d = 20; d <= 60; d+4) {
    console.log(d);
    if (d >= 50) {
        break
    }
}